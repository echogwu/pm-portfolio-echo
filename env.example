# Environment variable template (copy to `.env.local` if/when needed)
#
# This project currently does not require any environment variables to run.
# When you add integrations, define variables here and document them in CONTRIBUTING.md.
#
# Example:
# NEXT_PUBLIC_SITE_URL=http://localhost:3000


